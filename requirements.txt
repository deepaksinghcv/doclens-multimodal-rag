# DocLens dependencies. Use Python 3.12 (PyTorch/ChromaDB lack 3.13+ wheels).
#   python3.12 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
pymupdf==1.24.10
sentence-transformers==3.0.1
chromadb==0.5.5
ollama==0.3.3
numpy>=1.26,<2.0
# Gradio 4.x — 5/6 pull huggingface-hub>=1.0, which breaks transformers (needs <1.0).
gradio>=4.44,<5
huggingface-hub>=0.34,<1.0
# Gradio 4.44 targets the Sept-2024 web stack; newer FastAPI/Starlette/pydantic
# break its schema parsing and Starlette TemplateResponse call. Pin the window.
fastapi>=0.112,<0.113
pydantic>=2.8,<2.10
